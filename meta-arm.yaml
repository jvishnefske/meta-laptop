---
header:
  version: 1
repos:
   meta-arm:
     url: "https://git.yoctoproject.org/git/meta-arm"
     layers:
       meta-arm:
       meta-arm-bsp:
       meta-arm-toolchain:
local_conf_header:
  arm: |
      LICENSE_FLAGS_ACCEPTED += "armcompiler"
      TCMODE="external-arm"
      EXTERNAL_TOOLCHAIN = "${@bb.utils.contains('TUNE_FEATURES','aarch64','/work/arm-gnu-toolchain-12.2.rel1-x86_64-aarch64-none-linux-gnu', '/work/arm-gnu-toolchain-12.2.rel1-x86_64-arm-none-linux-gnueabihf',d)}"
  
